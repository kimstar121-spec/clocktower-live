import Link from "next/link"; import {characters} from "@/lib/characters"; import "../modes.css";
export default function Wiki(){return <main className="shell"><header><div className="mark">CT</div><div><b>CLOCKTOWER</b><span>백과사전</span></div></header><section className="hero"><p className="eyebrow">Trouble Brewing</p><h1>캐릭터 백과사전</h1><p>역할의 능력, 행동 시점과 승리 목표를 게임 전에 확인하세요.</p><Link className="secondary back" href="/">← 게임으로 돌아가기</Link></section><div className="encyclopedia">{characters.map(c=><article className={`card codex ${c.team}`} key={c.name}><div className="portrait" style={{backgroundPosition:`${(c.index%6)*20}% ${Math.floor(c.index/6)*33.333}%`}}/><div><small>{c.team} · {c.en}</small><h2>{c.name}</h2><p>{c.ability}</p><b>언제: {c.timing}</b><em>승리 목표: {c.goal}</em></div></article>)}</div></main>}
